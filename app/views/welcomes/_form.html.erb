<%= simple_form_for(@welcome) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, :label => false, :placeholder => 'ご相談内容をお書きください。', :hint => false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
